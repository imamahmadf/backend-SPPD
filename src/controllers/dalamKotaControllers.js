const {
  pegawai,
  golongan,
  pangkat,
  daftarTingkatan,
  daftarGolongan,
  daftarPangkat,
  daftarUnitKerja,
} = require("../models");

const { Op } = require("sequelize");

module.exports = {
  postSuratTugas: async (req, res) => {},
};
